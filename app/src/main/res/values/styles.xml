<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="DefaultTextWhite" parent="Widget.AppCompat.TextView">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">18sp</item>
    </style>
    
    <style name="SpinnerText" parent="DefaultTextWhite">
        <item name="android:padding">@dimen/default_size_10dp</item>
    </style>

    <style name="DefaultSpinnerStyle" parent="Widget.AppCompat.Light.DropDownItem.Spinner">
        <item name="backgroundTint">@android:color/white</item>
    </style>

</resources>
