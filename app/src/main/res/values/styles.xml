<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="colorControlNormal">@android:color/white</item>
    </style>

    <style name="DefaultTextGray" parent="Widget.AppCompat.TextView">
        <item name="android:textColor">@color/medium_gray</item>
        <item name="android:textSize">14sp</item>
    </style>

    <style name="SpinnerText" parent="DefaultTextGray">
        <item name="android:paddingTop">@dimen/default_size_10dp</item>
        <item name="android:paddingBottom">@dimen/default_size_10dp</item>
        <item name="android:inputType">textCapCharacters</item>
    </style>

    <style name="DefaultSpinnerStyle" parent="Widget.AppCompat.Light.DropDownItem.Spinner">
        <item name="backgroundTint">@android:color/holo_green_dark</item>
    </style>

    <style name="TranslateWordTextInputLayout" parent="Widget.Design.TextInputLayout">
        <item name="android:layout_marginTop">18dp</item>
        <item name="android:hint">@string/word_to_translate_hint</item>
    </style>

    <style name="TranslateWordEditText" parent="Widget.AppCompat.EditText">
        <item name="android:layout_marginTop">20dp</item>
        <item name="android:layout_marginStart">16dp</item>
        <item name="android:layout_marginEnd">16dp</item>
        <item name="android:background">@drawable/edit_text_shape</item>
        <item name="android:gravity">center</item>
        <item name="android:inputType">textMultiLine</item>
        <item name="android:lines">3</item>
        <item name="android:paddingStart">6dp</item>
        <item name="android:paddingTop">20dp</item>
        <item name="android:paddingEnd">6dp</item>
        <item name="android:textSize">18sp</item>
    </style>

    <style name="Word" parent="Widget.AppCompat.TextView">
        <item name="android:layout_marginTop">6dp</item>
        <item name="android:layout_marginStart">16dp</item>
        <item name="android:layout_marginEnd">16dp</item>
        <item name="android:textColor">@color/green</item>
        <item name="android:paddingStart">6dp</item>
        <item name="android:paddingTop">20dp</item>
        <item name="android:paddingEnd">6dp</item>
        <item name="android:textSize">50sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:gravity">center_horizontal</item>
    </style>

</resources>
